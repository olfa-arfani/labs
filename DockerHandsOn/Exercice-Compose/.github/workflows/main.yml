name: Continuous Integration Pipeline

on:
  push:
    branches: [ main, arthurvs ]
  pull_request:
    branches: [ main, arthurvs ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - name: Checkout code
       uses: actions/checkout@v2